services:
  slack-tnb:
    credentials:
      token: xoxb-some-slack-token-value
      channels:
        mc:
          name: mc
          webhook_url: some-webhook-url
          channel_id: some-channel-id
        general:
          name: general
          webhook_url: some-webhook-url
          channel_id: some-channel-id
  # Performance specific section, can be skipped for non performance tests
  horreum:
    credentials:
      tests:
        send-cloud-events-1-users:
          token: some-token
        send-cloud-events-4-users:
          token: some-token
        send-cloud-events-8-users:
          token: some-token