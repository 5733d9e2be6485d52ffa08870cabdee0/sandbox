services:
  slack-tnb:
    credentials:
      token: xoxb-some-slack-token-value
      channels:
        mc:
          name: mc
          webhook_url: some-webhook-url
          channel_id: some-channel-id
