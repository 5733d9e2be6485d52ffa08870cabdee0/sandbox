# The Kafka broker location (defaults to localhost:9092)
kafka.bootstrap.servers=localhost:9092

# Configuring the incoming channel (reading to Kafka)
mp.messaging.outgoing.events-in.connector=smallrye-kafka
mp.messaging.outgoing.events-in.topic=events
mp.messaging.outgoing.events-in.value.serializer=org.apache.kafka.common.serialization.StringSerializer
