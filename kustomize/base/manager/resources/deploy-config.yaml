apiVersion: v1
kind: ConfigMap
metadata:
  name: event-bridge-manager-config
data:
  EVENT_BRIDGE_DB_USERNAME: toReplace
  EVENT_BRIDGE_DB_PASSWORD: toReplace
  EVENT_BRIDGE_DB_HOST: toReplace
  EVENT_BRIDGE_DB_PORT: toReplace
  EVENT_BRIDGE_DB_SCHEMA: toReplace
  EVENT_BRIDGE_SSO_URL: toReplace
  EVENT_BRIDGE_SSO_CLIENT_ID: toReplace
  EVENT_BRIDGE_MANAGED_BRIDGE_DEPLOYMENT_TIMEOUT_SECONDS: "300"
  EVENT_BRIDGE_MANAGED_PROCESSOR_DEPLOYMENT_TIMEOUT_SECONDS: "300"