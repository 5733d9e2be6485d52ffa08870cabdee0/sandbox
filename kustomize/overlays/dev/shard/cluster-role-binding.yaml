apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: shard-operator-knative
roleRef:
  kind: ClusterRole
  apiGroup: rbac.authorization.k8s.io
  name: shard-operator-knative
subjects:
  - kind: ServiceAccount
    name: shard-operator-knative
    namespace: event-bridge-knative