services:
  slack-tnb:
    credentials:
      token: <SLACK_WEBHOOK_TOKEN>
      channels:
        mc:
          name: mc
          webhook_url: <SLACK_WEBHOOK_URL>
          channel_id: <SLACK_CHANNEL>
