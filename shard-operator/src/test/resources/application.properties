quarkus.scheduler.enabled=false
quarkus.container-image.build=false

event-bridge.ingress.image=openbridge/ingress:latest
event-bridge.executor.image=openbridge/executor:latest
event-bridge.manager.url=http://localhost:8080

event-bridge.auth-server-url=http://localhost:8081/auth/realms/event-bridge-fm
event-bridge.client-id=event-bridge
event-bridge.credentials.secret=secret
event-bridge.grant.type=password
event-bridge.grant-options.password.username=kermit
event-bridge.grant-options.password.password=thefrog

event-bridge.default.kafka.bootstrap.servers=localhost:9092
event-bridge.default.kafka.client.id=client-id
event-bridge.default.kafka.client.secret=secret
event-bridge.default.kafka.security.protocol=PLAINTEXT

