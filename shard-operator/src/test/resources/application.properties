quarkus.scheduler.enabled=false

# TODO: remove the following properties when the transitive dependency to ingress and executor (through k8s module) is removed
kafka.security.protocol=PLAINTEXT
kafka.sasl.jaas.config=none